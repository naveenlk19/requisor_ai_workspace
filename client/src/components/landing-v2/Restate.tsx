const Restate = () => (
  <section className="py-20 md:py-28 border-y border-border/60 bg-card/40">
    <div className="container max-w-5xl mx-auto text-center">
      <div className="label-mono mb-8">§ So what is Requisor, exactly</div>
      <p className="text-3xl md:text-5xl leading-[1.15] text-balance font-medium">
        Requisor is the <span className="text-accent-deep">context hub</span> for
        product teams — one place where every customer call, support ticket,
        sales note, and chat thread becomes <span className="text-accent-deep">structured, queryable evidence</span> that
        AI agents reason over to draft specs, rank work, and link every shipped
        feature back to the moment a customer asked for it.
      </p>
      <p className="mt-10 text-base md:text-lg text-muted-foreground max-w-2xl mx-auto leading-relaxed">
        Not a notes app. Not another roadmap tool. The missing data layer
        between your customers and your roadmap.
      </p>
    </div>
  </section>
);

export default Restate;
